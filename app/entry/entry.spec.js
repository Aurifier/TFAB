'use strict';

describe('myApp.entry module', function() {

  beforeEach(module('myApp.entry'));

  describe('entry controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var entryCtrl = $controller('EntryCtrl');
      expect(entryCtrl).toBeDefined();
    }));

  });
});
